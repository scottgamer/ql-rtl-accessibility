import React from "react";
import Child from "./Child";

const Parent = () => {
  return (
    <div>
      <h2>I'm the parent component</h2>
      <Child />
    </div>
  );
};

export default Parent;
